# This can safely reside in the git repo.
# Don't be an idiot and fill it with secure info.
DOMAIN='<:DOMAIN:>'
PUBLIC_PATH='https://<:CLIENT_S3_BUCKET:>.s3.amazonaws.com/'
# Serverless Framework config
SERVERLESS__PROFILE='<:AWS_PROFILE:>'
SERVERLESS__SITE_S3_BUCKET='<:SITE_S3_BUCKET:>'
SERVERLESS__HOSTED_ZONE='<:(value += props.DOMAIN.split('.').slice(-2).join('.')):>.'
SERVERLESS__CLIENT_S3_BUCKET='<:CLIENT_S3_BUCKET:>'
SERVERLESS__MEMORY_SIZE=1536