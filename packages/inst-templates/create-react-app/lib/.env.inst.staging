# This can safely reside in the git repo.
# Don't be an idiot and fill it with secure info.
CONFIG__DOMAIN='<:DOMAIN_STAGING:>'
CONFIG__PUBLIC_PATH='https://<:S3_BUCKET_STAGING:>.s3.amazonaws.com/'
# Serverless Framework config
SERVERLESS__PROFILE='<:AWS_PROFILE_STAGING:>'
SERVERLESS__S3_BUCKET='<:S3_BUCKET_PRODUCTION:>'
SERVERLESS__MEMORY_SIZE=768